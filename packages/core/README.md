# @character-ui/core

Provider-neutral schemas, validation, defaults, local-library helpers, and the deterministic `system-markdown-v1` compiler used by every Character UI surface.

The package performs no network requests and executes no imported content.
